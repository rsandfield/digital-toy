class_name AuxWire
extends Component

@onready var ropeMesh: RopeMesh = $Rope3D/RopeMesh

# func _physics_process(_delta):
	# var in_pos = inputs[0].global_position
	# var out_pos = outputs[0].global_position
	# ropeMesh.global_position = (in_pos + out_pos) / 2